services:
  web:
    build: .
    volumes:
      - ./service:/service
    ports:
      - '80:80'
    restart: on-failure

  worker-2:
    build: .
    volumes:
      - ./service:/service
    command: python manage.py via_parser
    restart: on-failure